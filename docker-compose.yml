networks:
  autotagger-service:
    driver: bridge

services:
  autotagger:
    build:
      context: .
      dockerfile: Dockerfile
    gpus: all
    user: "1000:1000"
    restart: always
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - MAX_INFLIGHT=2
      - WORKER_PROCESSES=2
      - MAX_UPLOAD_MB=32
      - MAX_FILE_MB=16
      - MAX_FILES=8
      - MAX_LIMIT=200
<<<<<<< HEAD
      - MODEL_PATH=/models/model.pth
=======
      - MODEL_PATH=models/model.pth
      - CUDNN_BENCHMARK=1
>>>>>>> 42339a90fe4e790e325a84e8543f3f722b6c4caa
      - BATCH_SIZE=32
      - MIN_BATCH_SIZE=1
      - GC_EVERY=50
      - EMPTY_CACHE_MIN_IMAGES=128
    ports:
      - "5000:5000"
    volumes:
      - .:/autotagger
      - ${AUTOTAGGER_MODEL_DIR:-/srv/autotagger-models}:/models:ro
    mem_limit: "2g"
    cpus: "1.0"
    shm_size: "2gb"
    networks:
      - default
      - autotagger-service
